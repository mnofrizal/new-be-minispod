# Environment variables for the application

# Application Configuration
NODE_ENV=development
PORT=3000
API_VERSION=v1

# Database Configuration
# Example: DATABASE_URL="postgresql://user:password@host:port/database"
DATABASE_URL="postgresql://postgres:password@100.90.149.26:5432/new_minispod"

# JWT Configuration
JWT_SECRET="your-jwt-secret-key"
JWT_EXPIRES_IN="24h"
JWT_REFRESH_SECRET="your-jwt-refresh-secret-key"
JWT_REFRESH_EXPIRES_IN="7d"

# Kubernetes Configuration (optional)
# Path to your kubeconfig file for connecting to a remote Kubernetes cluster.
# If not set, the app will try to use in-cluster config (in production)
# or the default kubeconfig location (~/.kube/config) in development.
# KUBECONFIG_PATH="/path/to/your/k3s.yaml"