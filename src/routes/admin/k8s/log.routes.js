import express from "express";

const router = express.Router();

// No routes needed here as Socket.IO handles its own connections.
// This file is for structure and future HTTP-based interactions.

export default router;
